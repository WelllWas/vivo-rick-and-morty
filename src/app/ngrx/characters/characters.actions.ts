// import { createAction, props } from '@ngrx/store';
// import { character } from 'src/app/utils/types';

// export const loadCharacter = createAction('[Character] Load Character');
// export const addCharacter = createAction('[Character] Add Character', props<{ character: character }>());
// export const editCharacter = createAction('[Character] Edit Character', props<{ id: number, character: character }>());
// export const removeCharacter = createAction('[Character] Remove Character', props<{ name: string }>());
